int transitionPoint(int arr[], int n) 

{
    int sum = 0;
    for(int i=0 ; i<n ; i++)
    {
        sum=sum+arr[i];
    }
    if(sum==0)
    return -1;
    else if(sum==n)
    return 0;
    else
    return n-sum;
}
